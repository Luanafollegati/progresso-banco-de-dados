SELECT * FROM calcados;

SELECT COUNT(*) As  total_calcados FROM calcados;

SELECT tipo, cor;
FROM calcados;

SELECT tipo, cor;
FROM calcados;
WHERE numero = '40';

SELECT tipo, cor;
FROM calcados;
WHERE numero = '45';

SELECT tipo, cor;
FROM calcados;
WHERE id = '4';

